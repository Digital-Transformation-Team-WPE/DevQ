# Copy this file to .env and fill in the values.

# Backend API base URL
# Leave empty in development — Vite proxy forwards /api/* to the ASP.NET Core backend.
# In production set this to the deployed backend origin:
#   VITE_API_BASE_URL=https://api.yourdomain.com
VITE_API_BASE_URL=
